services:
  orari-api:
    build: .
    container_name: naviturni-orari-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/orari.db
      - CORS_ORIGINS=*
    volumes:
      - ./data:/app/data
